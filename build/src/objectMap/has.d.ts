import { IMap, INumberMap } from "./interfaces";
export declare function has<TVal>(map: IMap<TVal>, key: string): boolean;
export declare function has<TVal>(map: INumberMap<TVal>, key: number): boolean;
